abstract class AppRoutes {
  // Splash Screen
  static const String splash = "/";

  // Homepage
  static const String pageOne = "/page-one";

  // Settings with arguments
  static const String pageTwo = "/page-two";

  // Profile with created custom arguments
  static const String pageThree = "/page-three";

   static const String pageFour = "/page-four";

  // define other routes
}